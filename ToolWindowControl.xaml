<UserControl x:Class="OdantDev.ToolWindow1Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="DeepPurple" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel>
        <StackPanel Orientation="Vertical" DockPanel.Dock="Top" Visibility="Visible" x:Name="spConnect">
            <TextBlock Margin="10" HorizontalAlignment="Center">ODANT</TextBlock>
            <Button Content="Connect" Click="Connect" Width="120" Height="31" Name="button1" Margin="10,100,10,10"/>
        </StackPanel>
        
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Visibility="Collapsed" x:Name="CommonButtons">
            <Button Width="125" Height="25" Name="RefreshTreeButton" FontSize="12" Click="button3_Click" Margin="5">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="Autorenew" Margin="0,0,5,0" Height="18" Width="18"/>
                    <TextBlock VerticalAlignment="Center" >Refresh Tree</TextBlock>
                </StackPanel>
            </Button>
            <Button Width="100" Height="25" Name="button4" FontSize="12" Click="button3_Click" Margin="5">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="FolderPlusOutline" Margin="0,0,5,0" Height="18" Width="18"/>
                    <TextBlock VerticalAlignment="Center" >Create</TextBlock>
                </StackPanel>
        </Button>
            <Button Content="Download" Width="100" Height="25" Name="button2" FontSize="12" Click="button2_Click" Margin="5"/>
            <Button Content="Open" Width="100" Height="25" Name="button3" FontSize="12" Click="button3_Click_1" />
            <Label Margin="5">Разработчик:</Label>
            <ComboBox Margin="5" SelectedValue="{Binding OdaModel.Developers[0]}" ItemsSource="{Binding OdaModel.Developers}" x:Name="Developers"></ComboBox>
        </StackPanel>
        <!--<WindowsFormsHost x:Name="winHost"></WindowsFormsHost>-->

        <TreeView x:Name="OdaTree" ItemsSource="{Binding OdaModel.Nodes}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Path=Icon}" Width="16" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding }" FontSize="14"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </DockPanel>
</UserControl>
